# This file marks the folder as an importable Python package.

"""`accounts` Django app package initializer.

This file intentionally left minimal. It marks the directory as a
Python package so Django can discover app modules (models, views, etc.).
Add app-level initialization here only if necessary.
"""
