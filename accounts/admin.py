# This module configures how data appears in Django's admin site.

# Django admin registration placeholder.
# Register models from `accounts.models` here to make them editable via
# the Django admin site. This file is intentionally minimal.
