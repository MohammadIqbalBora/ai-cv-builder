# This module defines the data stored in the database.

# This app uses Django's built-in User model, imported in accounts/forms.py,
# so it does not currently need a custom account database model here.
# Create your models here.
